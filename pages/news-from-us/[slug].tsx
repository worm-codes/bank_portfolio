import Layout from 'layout';
import { NewsDetails } from 'modules';

export default function NewsDetailPage() {
  return (
    <Layout meta={{ title: 'Anonymous Bank' }}>
      <NewsDetails />
    </Layout>
  );
}
