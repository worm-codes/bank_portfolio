import Layout from 'layout';
import { AboutUs } from 'modules';

const AboutPage = () => {
  return (
    <Layout meta={{ title: 'About Us' }}>
      <AboutUs />
    </Layout>
  );
};

export default AboutPage;
